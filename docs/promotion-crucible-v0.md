# Promotion/Invariance Crucible v0

## Result

The founding candidate does not survive translation as one indivisible invariant.

```text
EPISTEMIC:  supports
HISTORY:    only-one-domain
AUTHORITY:  supports
CONSEQUENCE: only-one-domain
IDENTITY:   supports
WHOLE:      split-required
```

This is an observed lawful refusal, not a target label embedded in the case. There is no average, percentage, or aggregate green: the emitted `aggregate_score` is `null`, and the whole is derived by conjunction. Three supporting clauses plus two local clauses still require a split.

The executable receipt is [`specimens/promotion-crucible-v0/receipt.json`](../specimens/promotion-crucible-v0/receipt.json). Its case digest is:

```text
sha256:d97b1102dae7217d61d4f86f50d296e3f6b1e03dfff2b636417c9680f42cbc0f
```

## What survived translation

### Epistemic clause — `supports`

> Projection alone does not increase what the underlying evidence identifies.

Free Graph's local relational-coordinate fixture preserves the compatible-world set when it re-expresses an unchanged observation.

National Treasure independently reaches the same observable constraint in finite graph vocabulary. Its native `ssw-math.receipt/v0` shows that `C6` and two disjoint triangles have the same supplied vertex count, edge count, and degree sequence; deterministic re-expressions preserve that collision. Only the new connectedness discriminator separates them.

National Treasure imports none of Free Graph's code, schema, constitutional vocabulary, or receipt shape.

### Identity clause — `supports`

> Representation or lens change does not silently become object or worldline change.

Free Graph's view operation preserves object and worldline IDs; a changed object requires an explicit transform receipt.

National Treasure preserves two non-isomorphic graph identities even though their supplied observations coincide. Equality of representation does not merge the candidate worlds.

## Existing-system harvest

The crucible searched systems that predated it before constructing any clause-specific specimen. Their native receipts were copied byte-for-byte and adapted only at the evaluator boundary; none were rewritten into crucible nouns.

| Existing artifact | Authority | Consequence | History | Why it does or does not count |
| --- | --- | --- | --- | --- |
| Full Bowl 001 original receipt | `unresolved` | `unresolved` | `unresolved` | Its own leak ledger says admission, authority, attempt, and outcome are compressed, and the genuine warrant/terminal receipt is unavailable for same-specimen causal reconciliation. It is preserved as a strict non-witness. |
| Full Bowl 002 repaired-world receipt | `satisfies` | `unresolved` | `unresolved` | Its Corpus OS-owned closure exposes ordered `destination-admission`, `local-authority`, `attempt`, and `outcome` phases while retaining `authorityTransfer: none`. However, `FB001-U001` explicitly withholds the genuine warrant and terminal receipt needed for same-specimen causal reconciliation; it also contains no decoder succession. |
| Haunted Toaster existing raster/archive tests | not tested | not tested | near match, not counted | Old rasters remain replayable and archived bytes remain immutable, but no native existing artifact pairs two renderer versions for the same source with an explicit derivation edge. Stitching those facts together would manufacture the missing witness. |

The strongest second witness here did not know it was applying for the job. Conversely, a near match is not promoted by interpretation.

### Authority clause — `supports`

Free Graph demonstrates that lens selection leaves effective warrant unchanged, while a warrant increase is lawful only through a separately attributable owner-local gate. Full Bowl 002 independently exposes a native `local-authority` admission owned by Corpus OS between destination admission and execution. The original Full Bowl 001 receipt is not counted: its boundary compressed the very phases needed to decide the clause.

The Full Bowl receipt retains its own schema, event names, ownership, and receipt shape. The adapter observes those native events; it does not remodel them into `projectionAuthority` declarations.

### Consequence clause — `only-one-domain`

> Projection alone does not constitute a consequential state change; any consequence requires an independently named local gate.

The Free Graph fixture includes both sides:

- projection with no state change and no gate;
- projection observed in a lawful causal path whose state change is attributed to a separate local gate.

It also refuses an ungated state change. This does not make projections causally inert.

Full Bowl 002 is a valuable near witness, but not a completed consequence witness. Its native receipt orders `local-authority`, `attempt`, and `outcome` phases under a Corpus OS-owned closure. The same receipt nevertheless preserves `FB001-U001`: the stdio boundary does not expose the genuine warrant and terminal receipt needed for same-specimen causal reconciliation. Calling that consequence `satisfies` from the phase silhouette alone would overwrite the artifact's own unresolved semantics.

An adversarial test removes the `FB001-U001` label while leaving the phase silhouette unchanged; consequence remains unresolved. Deleting a refusal is not positive causal evidence.

Semantic fracture: the local gate order is visible, but the attributable same-specimen transition is not independently reconstructible from the native receipt.

Missing discriminator: find an existing unrelated system whose native receipt preserves both sides of one causal chain—representation may inform the system, while a separately attributable decision or commit event demonstrably owns the actual state transition.

## What did not yet survive translation

### Historical clause — `only-one-domain`

Free Graph preserves the prior projection ID, content digest, and decoder version byte-for-byte while appending a newly identified, newly versioned projection. Reusing or rewriting the old ID is refused.

Semantic fracture: National Treasure and both Full Bowl receipts lack decoder-version succession. Haunted Toaster supplies independently built replayability and immutable archival behavior, but no existing native artifact pairs an old and new rendering of the same source under two renderer versions with explicit derivation attribution.

Missing discriminator: find an existing unrelated system with separately addressable old and new renderings of the same source under two interpreter, renderer, or decoder versions, plus a native derivation edge from the new rendering to the old. No history-specific specimen should be invented before that search is exhausted.

## Anti-circularity control

The case contains behavior and source pins, not an expected verdict. The evaluator rejects verdict-ground-truth keys such as `expected`, `verdict`, and `expected_verdict` anywhere in the input.

The tests then mutate behavior and observe the evaluator move the same epistemic clause through three outcomes:

```text
two independent satisfying behaviors -> supports
remove the independent behavior       -> only-one-domain
make a positive behavior contradict   -> refuses
```

The committed receipt is compared for determinism only after its verdicts have been derived. It is an output oracle, not an input oracle.

## Mandatory poison

`poison_consumer.py` is a standalone implementation that imports no Free Graph code. It deliberately emits the copied receipt silhouette and declarations:

```json
{
  "projectionAuthority": "none",
  "projectionConsequence": "requires-local-gate"
}
```

Its behavior does the reverse: observing its projection raises effective warrant to `owner-local`, unlocks the action, and changes `locked` to `unlocked` with an empty `gateReceipts` list.

The crucible ignores the declarations and evaluates the operational trace. It refuses both `authority` and `consequence`; the mandatory attack outcome is `caught`. The generated poison receipt is digest-pinned:

```text
sha256:3b054675a57ea2a4329d799d9e352e50cc61d112dc3383bd584106b3af5abaab
```

The poison is an integrity attack, never a positive portability embodiment.

## Source floor

| Source | Immutable pin | Role |
| --- | --- | --- |
| Free Graph relational-coordinate pressure | `50f8c5298150f04e7bf08dc29b70e500ccb36880` | local five-clause embodiment |
| National Treasure SSW-MATH-001 | `65e67062a3be933332db36af8bc095fc9b2660b4` | independent epistemic and identity embodiment |
| Native SSW receipt | `sha256:45ae6d1ff7fb29358320ff637d53a3594422d60d68ace7aa4546fc25ecbfb447` | unedited native testimony |
| Full Bowl 001 and 002 canonical integration commit | `5f6e0b9460859a26868bc2f58f6693d6734e1940` | pre-existing authority/consequence pressure and repaired native phase receipt |
| Native Full Bowl 001 receipt | `sha256:55d6431b7e5acd03bb5bf05f17e7dd4e3e5227803fdc5e54a941470b68c17199` | unedited strict non-witness |
| Native Full Bowl 002 receipt | `sha256:25f8b0f4f45ffc1be652f56da6d46da450742ab4c23b6ec847548c90a17c50ee` | unedited authority testimony and unresolved consequence pressure |
| Blackwell (1953) | [`10.1214/aoms/1177729032`](https://doi.org/10.1214/aoms/1177729032) | research lineage for experiment comparison and stochastic transformation |

Blackwell supplies conceptual ancestry, not the executable graph proof and not the repository-local constitutional clauses. The finite witness and its minimum discriminator are checked independently in National Treasure.

## Reproduce

```bash
python3 specimens/promotion-crucible-v0/poison_consumer.py \
  --output specimens/promotion-crucible-v0/poison-receipt.json

python3 scripts/promotion_crucible.py \
  specimens/promotion-crucible-v0/case.json \
  --output specimens/promotion-crucible-v0/receipt.json

python3 -m unittest tests.test_promotion_crucible -v
```

Repeated runs emit byte-identical receipts.

## Contract boundary

- The harness format is `promotion-crucible.case/v0`; it is not a Free Graph packet schema change.
- The five Free Graph verbs remain unchanged.
- The result is testimony about translation pressure, not adoption by an owner project.
- Free Graph issue #2 remains open; this harness consumes its pressure but does not pretend to complete its full specimen.
- The combined candidate remains split while consequence and history each have only one independently material embodiment.
